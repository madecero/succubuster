{
  "name": "succubuster",
  "private": true,
  "version": "0.1.0",
  "description": "On-device suggestive-engagement-bait filter for X. Fully client-side: text heuristics + CLIP zero-shot + a trained ONNX head. No external inference API.",
  "workspaces": [
    "shared",
    "extension"
  ],
  "scripts": {
    "build": "npm run build -w @succubuster/extension",
    "download-models": "node extension/scripts/download-models.mjs",
    "package": "npm run build && npm run zip -w @succubuster/extension"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "tsx": "^4.22.4"
  }
}
